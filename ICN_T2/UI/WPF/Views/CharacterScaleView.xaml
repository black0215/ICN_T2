<UserControl x:Class="ICN_T2.UI.WPF.Views.CharacterScaleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:ICN_T2.UI.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100"
             ClipToBounds="False"
             Background="Transparent"
             TextElement.Foreground="#1F1F1F">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/PuniPuniTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <conv:NullToVisibilityConverter x:Key="NullToVisibility"/>
            <Style x:Key="ScaleValueBoxStyle" TargetType="TextBox">
                <Setter Property="FontFamily" Value="Consolas"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="MinWidth" Value="78"/>
                <Setter Property="Padding" Value="6,3"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="RootGrid"
          SnapsToDevicePixels="True"
          UseLayoutRounding="True"
          ClipToBounds="False"
          TextOptions.TextFormattingMode="Display"
          TextOptions.TextRenderingMode="ClearType"
          TextOptions.TextHintingMode="Fixed">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="70*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,90,0,0">
            <Border CornerRadius="24"
                    Tag="ToolPanelBackdropGlass"
                    IsHitTestVisible="False"/>
            <Border Background="#33FFFFFF"
                    BorderBrush="#22FFFFFF"
                    BorderThickness="1"
                    CornerRadius="24"
                    Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Character Scale"
                               FontSize="20"
                               FontFamily="Segoe UI"
                               FontWeight="Bold"
                               Foreground="#1F1F1F"
                               Margin="0,0,0,16"/>

                    <Border Grid.Row="1"
                            Margin="0,0,0,16"
                            Height="44"
                            Style="{StaticResource IosSearchFieldStyle}">
                        <Grid>
                            <TextBlock Text="Search..."
                                       FontSize="14"
                                       Foreground="#601F1F1F"
                                       Margin="14,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=TxtSearch}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBox x:Name="TxtSearch"
                                     Padding="14,0"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     FontSize="14"
                                     VerticalContentAlignment="Center"
                                     Foreground="#1F1F1F"/>
                        </Grid>
                    </Border>

                    <ListBox Grid.Row="2"
                             ItemsSource="{Binding FilteredList}"
                             SelectedItem="{Binding SelectedScale, Mode=TwoWay}"
                             Background="Transparent"
                             BorderThickness="0"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             VirtualizingPanel.ScrollUnit="Pixel"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource IosListItemStyle}">
                                <Setter Property="Foreground" Value="#1F1F1F"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="40" Height="40" CornerRadius="10" ClipToBounds="True" Background="#20000000" Margin="0,0,12,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Image Grid.Column="0" Source="{Binding RankIcon}" Width="16" Height="16" VerticalAlignment="Center" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True"/>
                                            <Image Grid.Column="1" Source="{Binding TribeIcon}" Width="16" Height="16" VerticalAlignment="Center" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True"/>
                                        </Grid>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding BaseHash, StringFormat='ID: {0:X8}'}" FontSize="11" FontFamily="Consolas" Opacity="0.62" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Column="2"
              Margin="0,-5,0,-20"
              ClipToBounds="False"
              Visibility="{Binding SelectedScale, Converter={StaticResource NullToVisibility}}">
            <Border CornerRadius="28"
                    Tag="ToolPanelBackdropGlass"
                    IsHitTestVisible="False"/>
            <Border Background="#33FFFFFF"
                    BorderBrush="#22FFFFFF"
                    BorderThickness="1"
                    CornerRadius="28"
                    Padding="14">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Margin="0">
                    <StackPanel>
                        <Border Background="#40FFFFFF"
                                BorderBrush="#15FFFFFF"
                                BorderThickness="1"
                                CornerRadius="24"
                                Padding="14"
                                Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Width="72" Height="72" CornerRadius="18" Background="#24000000" ClipToBounds="True" Margin="0,0,14,0">
                                    <Image x:Name="ImgCharacterIcon"
                                           Stretch="UniformToFill"
                                           RenderOptions.BitmapScalingMode="HighQuality"
                                           SnapsToDevicePixels="True"/>
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding SelectedScale.Name}"
                                               FontSize="24"
                                               FontWeight="Bold"
                                               Foreground="#1F1F1F"
                                               Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal" Opacity="0.68">
                                        <TextBlock Text="Base: " FontSize="12"/>
                                        <TextBlock Text="{Binding SelectedScale.BaseHash, StringFormat='0x{0:X8}'}" FontFamily="Consolas" FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#40FFFFFF" BorderBrush="#15FFFFFF" BorderThickness="1" CornerRadius="20" Padding="12" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Body Group A" FontSize="11" FontWeight="Bold" Opacity="0.58" Margin="0,0,0,8"/>
                                    <TextBlock Text="Scale 1" FontSize="12" Opacity="0.78"/>
                                    <TextBox Text="{Binding SelectedScale.Scale1, UpdateSourceTrigger=LostFocus, StringFormat=F3}" Style="{StaticResource ScaleValueBoxStyle}" Margin="0,4,0,8"/>
                                    <TextBlock Text="Scale 2" FontSize="12" Opacity="0.78"/>
                                    <TextBox Text="{Binding SelectedScale.Scale2, UpdateSourceTrigger=LostFocus, StringFormat=F3}" Style="{StaticResource ScaleValueBoxStyle}" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Background="#40FFFFFF" BorderBrush="#15FFFFFF" BorderThickness="1" CornerRadius="20" Padding="12" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Body Group B" FontSize="11" FontWeight="Bold" Opacity="0.58" Margin="0,0,0,8"/>
                                    <TextBlock Text="Scale 3" FontSize="12" Opacity="0.78"/>
                                    <TextBox Text="{Binding SelectedScale.Scale3, UpdateSourceTrigger=LostFocus, StringFormat=F3}" Style="{StaticResource ScaleValueBoxStyle}" Margin="0,4,0,8"/>
                                    <TextBlock Text="Scale 4" FontSize="12" Opacity="0.78"/>
                                    <TextBox Text="{Binding SelectedScale.Scale4, UpdateSourceTrigger=LostFocus, StringFormat=F3}" Style="{StaticResource ScaleValueBoxStyle}" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="0" Grid.ColumnSpan="3" Background="#40FFFFFF" BorderBrush="#15FFFFFF" BorderThickness="1" CornerRadius="20" Padding="12" Margin="0,110,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Scale 5" FontSize="12" Opacity="0.78"/>
                                        <TextBox Text="{Binding SelectedScale.Scale5, UpdateSourceTrigger=LostFocus, StringFormat=F3}" Style="{StaticResource ScaleValueBoxStyle}" Margin="0,4,0,0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Scale 6" FontSize="12" Opacity="0.78"/>
                                        <TextBox Text="{Binding SelectedScale.Scale6, UpdateSourceTrigger=LostFocus, StringFormat=F3}" Style="{StaticResource ScaleValueBoxStyle}" Margin="0,4,0,0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="4">
                                        <TextBlock Text="Scale 7" FontSize="12" Opacity="0.78"/>
                                        <TextBox Text="{Binding SelectedScale.Scale7, UpdateSourceTrigger=LostFocus, StringFormat=F3}" Style="{StaticResource ScaleValueBoxStyle}" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
