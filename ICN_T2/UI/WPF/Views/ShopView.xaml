<UserControl x:Class="ICN_T2.UI.WPF.Views.ShopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100"
             ClipToBounds="False"
             Background="Transparent"
             TextElement.Foreground="#1F1F1F"
             TextElement.FontFamily="{DynamicResource GmarketSansMedium}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/PuniPuniTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="RootGrid"
          SnapsToDevicePixels="True"
          UseLayoutRounding="True"
          ClipToBounds="False"
          TextOptions.TextFormattingMode="Display"
          TextOptions.TextRenderingMode="ClearType">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="70*"/>
        </Grid.ColumnDefinitions>

        <!-- Shop File List -->
        <Grid Grid.Column="0" Margin="0,90,0,0">
            <Border CornerRadius="24" Tag="ToolPanelBackdropGlass" IsHitTestVisible="False"/>
            <Border Background="#33FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1"
                    CornerRadius="24" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="상점 목록 (Shops)"
                               FontSize="20" FontWeight="Bold"
                               Foreground="#1F1F1F"
                               Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="1"
                               Text="{Binding Shops.Count, StringFormat='총 {0}개 그룹'}"
                               FontSize="12" Opacity="0.65"
                               Margin="0,0,0,12"/>

                    <ListBox Grid.Row="2"
                             ItemsSource="{Binding Shops}"
                             SelectedItem="{Binding SelectedShop, Mode=TwoWay}"
                             Background="Transparent"
                             BorderThickness="0"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             VirtualizingPanel.ScrollUnit="Pixel"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource IosListItemStyle}">
                                <Setter Property="Foreground" Value="#1F1F1F"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="{Binding Items.Count, StringFormat='상품 {0} 개'}" FontSize="11" Opacity="0.6"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- Shop Detail Panel -->
        <Grid Grid.Column="2" Margin="0,-15,0,-30" ClipToBounds="False">
            <Border CornerRadius="28" Tag="ToolPanelBackdropGlass" IsHitTestVisible="False"/>
            <Border Background="#33FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1"
                    CornerRadius="28" Padding="14">
                <Grid DataContext="{Binding SelectedShop}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border Grid.Row="0" Background="#40FFFFFF" BorderBrush="#15FFFFFF" BorderThickness="1"
                            CornerRadius="24" Padding="14" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="{Binding DisplayName, FallbackValue='상점을 선택하세요'}" 
                                       FontSize="22" FontWeight="Bold" Margin="0,0,0,2"/>
                            <TextBlock Text="{Binding Items.Count, StringFormat='판매 물품: {0} 종'}" 
                                       FontSize="13" Opacity="0.7"/>
                        </StackPanel>
                    </Border>

                    <!-- Items DataGrid / List -->
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Items}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              Background="Transparent"
                              RowBackground="Transparent"
                              AlternatingRowBackground="#10000000"
                              BorderThickness="0"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#22000000"
                              RowHeight="40"
                              HeadersVisibility="Column"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto">
                        
                        <DataGrid.Resources>
                            <!-- Minimal DataGrid Style -->
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#1F1F1F"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="8"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#44000000"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="4"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter VerticalAlignment="Center" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <!-- Item Name (ComboBox) -->
                            <DataGridTemplateColumn Header="아이템 (Item)" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.ItemOptions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  DisplayMemberPath="Name"
                                                  SelectedValuePath="Key"
                                                  SelectedValue="{Binding ItemHash, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  Style="{StaticResource IosFilterComboBoxStyle}"
                                                  Margin="2" Padding="6,2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Item Hash (Read-Only) -->
                            <DataGridTextColumn Header="아이템 해시" 
                                                Binding="{Binding ItemHash, StringFormat='0x{0:X8}'}" 
                                                Width="100" IsReadOnly="True" 
                                                Foreground="#A0000000"/>

                            <!-- Price -->
                            <DataGridTemplateColumn Header="판매 가격 (Price)" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Price, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 HorizontalContentAlignment="Center"
                                                 BorderThickness="0"
                                                 Background="Transparent"
                                                 Margin="2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
