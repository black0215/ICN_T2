<UserControl x:Class="ICN_T2.UI.WPF.Views.Controls.SkillSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:ICN_T2.UI.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="300">
    
    <UserControl.Resources>
        <conv:StringEmptyToVisibilityConverter x:Key="StringEmptyToVisibility"/>
        <!-- iOS 26 Concept Style Resources -->
        <SolidColorBrush x:Key="SoftWhiteBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="GlassPanelBrush" Color="#15FFFFFF"/> <!-- Translucent background -->
        <SolidColorBrush x:Key="PopupBackgroundBrush" Color="#CC101010"/> <!-- Dark translucent popup -->
        
        <SolidColorBrush x:Key="SkillBorderGradient" Color="#FF0B111D"/>
        
        <!-- Button-like List Item Style -->
        <Style x:Key="SkillItemContainerStyle" TargetType="ComboBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" 
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="1"
                                CornerRadius="12"
                                Margin="4,2"
                                Padding="10,8">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#25FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#40FFFFFF"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#80FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{StaticResource SoftWhiteBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Toggle Button Style (The main display) -->
        <ControlTemplate x:Key="SkillToggleTemplate" TargetType="ToggleButton">
            <Border x:Name="MainBorder"
                    Background="{StaticResource GlassPanelBrush}"
                    BorderBrush="{StaticResource SkillBorderGradient}"
                    BorderThickness="1.2"
                    CornerRadius="14"
                    Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ContentPresenter Grid.Column="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}"
                                      VerticalAlignment="Center" HorizontalAlignment="Left"
                                      TextBlock.Foreground="{StaticResource SoftWhiteBrush}"
                                      TextBlock.FontWeight="Medium"/>
                    
                    <Path Grid.Column="1" Data="M0,0 L5,5 L10,0" Stroke="{StaticResource SoftWhiteBrush}" StrokeThickness="2" 
                          VerticalAlignment="Center" Margin="8,0,0,0" Opacity="0.6"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="MainBorder" Property="Background" Value="#25FFFFFF"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="MainBorder" Property="Background" Value="#30FFFFFF"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- ComboBox -->
        </Grid.RowDefinitions>

        <!-- Header Label -->
        <TextBlock Text="{Binding Header, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="{StaticResource SoftWhiteBrush}"
                   FontSize="12"
                   Opacity="0.7"
                   Margin="4,0,0,4"
                   Visibility="{Binding Header, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource StringEmptyToVisibility}}"/>

        <!-- Custom ComboBox Structure -->
        <ComboBox Grid.Row="1"
                  ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectedValue="{Binding SelectedValue, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                  DisplayMemberPath="{Binding DisplayMemberPath, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectedValuePath="{Binding SelectedValuePath, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            
            <ComboBox.Style>
                <Style TargetType="ComboBox">
                    <Setter Property="ItemContainerStyle" Value="{StaticResource SkillItemContainerStyle}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBox">
                                <Grid>
                                    <ToggleButton Name="ToggleButton" 
                                                  Template="{StaticResource SkillToggleTemplate}" 
                                                  Focusable="false"
                                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                                    
                                    <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                        <Grid Name="DropDown" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                            <Border x:Name="DropDownBorder" 
                                                    Background="{StaticResource PopupBackgroundBrush}"
                                                    BorderBrush="#30FFFFFF"
                                                    BorderThickness="1"
                                                    CornerRadius="14"
                                                    Margin="0,6,0,0"
                                                    Padding="4,6">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.4" Color="Black"/>
                                                </Border.Effect>
                                                <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                                </ScrollViewer>
                                            </Border>
                                        </Grid>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ComboBox.Style>
        </ComboBox>
        
    </Grid>
</UserControl>
