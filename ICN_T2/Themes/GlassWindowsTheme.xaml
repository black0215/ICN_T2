<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->
    <!-- 🎨 iOS 26 Glassmorphism Color Palette -->
    <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->

    <!-- Primary Acrylic Background (Light Blue-Gray) -->
    <SolidColorBrush x:Key="Acrylic_PrimaryBackground" Color="#D8E8F5F8"/>

    <!-- Border Colors -->
    <SolidColorBrush x:Key="Acrylic_BorderBrush" Color="#15FFFFFF"/>
    <SolidColorBrush x:Key="Acrylic_BorderHover" Color="#30FFFFFF"/>
    <SolidColorBrush x:Key="Acrylic_BorderSelected" Color="#50FFFFFF"/>

    <!-- Text Colors -->
    <SolidColorBrush x:Key="Acrylic_TextPrimary" Color="#1A1C1E"/>
    <SolidColorBrush x:Key="Acrylic_TextSecondary" Color="#777777"/>
    <SolidColorBrush x:Key="Acrylic_TextPlaceholder" Color="#999999"/>

    <!-- Input Field Colors -->
    <SolidColorBrush x:Key="Acrylic_InputBackground" Color="#30000000"/>
    <SolidColorBrush x:Key="Acrylic_InputHover" Color="#30000000"/>
    <SolidColorBrush x:Key="Acrylic_InputFocus" Color="#30000000"/>

    <!-- Shadow Color -->
    <Color x:Key="Acrylic_ShadowColor">#FF1A1C1E</Color>

    <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->
    <!-- 🎨 Shared Styles -->
    <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->

    <!-- 1. Acrylic Card Style (for all panels) -->
    <Style x:Key="AcrylicCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Acrylic_PrimaryBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Acrylic_BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1.5"/>
        <Setter Property="CornerRadius" Value="24"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="{StaticResource Acrylic_ShadowColor}"
                                  Opacity="0.08"
                                  BlurRadius="40"
                                  ShadowDepth="8"
                                  Direction="270"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 2. Acrylic TextBox Style -->
    <Style x:Key="AcrylicTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource Acrylic_InputBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource Acrylic_TextPrimary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontFamily" Value="Malgun Gothic, Segoe UI"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Acrylic_InputHover}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Acrylic_InputFocus}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 3. Acrylic ListBox Style -->
    <Style x:Key="AcrylicListBoxStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
    </Style>

    <!-- 4. Acrylic ListBoxItem Style -->
    <Style x:Key="AcrylicListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0,4"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="16"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#60FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 5. Acrylic ScrollBar Style -->
    <Style x:Key="AcrylicScrollBarStyle" TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid>
                        <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate>
                                            <Border Background="#40000000" CornerRadius="4"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 6. Acrylic Button Style -->
    <Style x:Key="AcrylicButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Acrylic_InputBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource Acrylic_TextPrimary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="FontFamily" Value="Malgun Gothic, Segoe UI"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="12"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Acrylic_InputFocus}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 7. Acrylic TextBlock Style (Headers) -->
    <Style x:Key="AcrylicHeaderTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Acrylic_TextPrimary}"/>
        <Setter Property="FontFamily" Value="Malgun Gothic, Segoe UI"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- 8. Acrylic TextBlock Style (Body) -->
    <Style x:Key="AcrylicBodyTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Acrylic_TextPrimary}"/>
        <Setter Property="FontFamily" Value="Malgun Gothic, Segoe UI"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
    </Style>

    <!-- 9. Acrylic TextBlock Style (Secondary) -->
    <Style x:Key="AcrylicSecondaryTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Acrylic_TextSecondary}"/>
        <Setter Property="FontFamily" Value="Malgun Gothic, Segoe UI"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Normal"/>
    </Style>

</ResourceDictionary>
