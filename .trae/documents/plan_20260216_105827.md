I will optimize the icon loading performance to eliminate UI lag and ensure smooth navigation.

### 1. Implement Global Icon Cache (`IconCache.cs`)

* **New Method**: Add `GetYokaiIcon(IGame game, CharaBase baseInfo)`.

* **Thread Safety**: Use `ConcurrentDictionary<string, BitmapImage>` to store loaded icons.

* **Logic**:

  * Check cache first.

  * If missing, load from game files (`LoadYokaiIconFromGame`) or fallback to external PNGs.

  * Store result in cache.

* **Benefit**: Eliminates redundant file I/O and supports concurrent access.

### 2. Update ViewModel (`YokaiStatsViewModel.cs`)

* **`YokaiStatsWrapper`** **Class**:

  * **Constructor**: Remove the synchronous `LoadIcon()` call. This ensures the ViewModel initializes instantly when the "Yokai Stats" button is clicked, allowing the entry animation to play smoothly without freezing.

  * **`Icon`** **Property**: Implement lazy loading.

    * On first access (when the list item binds), check if the icon is loaded.

    * If not, start a `Task.Run` to fetch from `IconCache`.

    * Update the property and notify the UI (`OnPropertyChanged`) upon completion.

* **Benefit**: Unblocks the UI thread, allowing immediate view switching and smooth scrolling.

### 3. Enable UI Virtualization (`YokaiStatsView.xaml`)

* **ListBox Optimization**:

  * Set `VirtualizingPanel.IsVirtualizing="True"`.

  * Set `VirtualizingPanel.VirtualizationMode="Recycling"`.

  * Ensure `ScrollViewer.CanContentScroll="True"`.

* **Benefit**: Only renders and loads icons for items currently visible on screen, significantly reducing memory usage and processing time.

### 4. Verification

* **Performance**: Confirm the "Yokai Stats" view opens instantly.

* **Scrolling**: Verify smooth 60 FPS scrolling through the list.

* **Caching**: Ensure icons load once and persist.

